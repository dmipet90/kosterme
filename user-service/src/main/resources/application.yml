server:
  port: ${PORT:0}
gateway:
  ip: 192.168.8.159
token:
  expiration-time: 3600000
  secret: d7m0hha7sbtfa71au1fki7e5flpq9pl6v3p5bplsblm8yeze0da3khkxwvbxl16j
login:
  url-path: /user/login
spring:
  application:
    name: user-ws
  devtools:
    restart:
      enabled: true
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8010/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance-id:${random.value}}
